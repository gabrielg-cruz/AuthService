# DataBase configuration:
spring.datasource.url=jdbc:mysql://localhost:3306/${DOTENV:MYSQL_DATABASE}?createDatabaseIfNotExist=true
spring.datasource.username=${DOTENV:MYSQL_USER}
spring.datasource.password=${DOTENV:MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#spring.security.user.name=admin
#spring.security.user.password=admin
#spring.security.user.roles=USER


#spring.security.user.name=//${ADMIN_USER}
#spring.security.user.password=${ADMIN_PASSWORD}
#spring.security.user.roles=${ADMIN_ROLE}

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.application.name=quickAuth

dotenv.enabled=true

logging.level.org.springframework.security=DEBUG